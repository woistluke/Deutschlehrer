# Improvement Log

Append-only log of daily review proposals for the Auf Deutsch app and their outcomes. Each entry is date-stamped. Status values: proposed, approved, implemented, rejected, deferred.

---

## 2026-07-10 — implemented (all 6 approved same-day, commit 3d2e4a7)

1. **Fix: Conversation tab loses chat history on "Back to chat."** In `js/pages/conversation.js`, `renderChat()` unconditionally creates a brand-new `createRichChat()` instance (and re-sends an opening greeting) every time the chat screen re-mounts — including when returning from flashcards or the session-review screen. Checking your vocab flashcards mid-conversation currently wipes the whole conversation instead of resuming it. Only the "change topic" path should reset; the flashcards round-trip should preserve the existing chat instance/history. Effort: small-medium. Files: `js/pages/conversation.js`.

2. **Reliability: no timeout on AI calls.** `js/ai.js`'s `chatCompletion`/`speak`/`transcribe` have no request timeout, so a hung Groq/OpenAI request leaves the UI stuck on "Denke nach…" / "Checking…" / "Grading…" indefinitely with no recovery besides a full reload. Add an `AbortController`-based timeout (~20-30s) with a clear, retryable error. Effort: small-medium. Files: `js/ai.js`.

3. **Consistency: sentence-drill phase skips forward on failure instead of offering retry.** In `js/pages/runner.js`, `mountSentencePhase()`'s catch block only offers "Skip to conversation," silently dropping the whole sentence-practice phase on a transient network error — whereas the quiz phase (`mountQuizPhase`) offers a proper "Try again" retry for the same class of failure. Mirror the quiz phase's retry pattern here. Effort: small. Files: `js/pages/runner.js`.

4. **Cleanup: stray backup file committed to the repo.** `js/seed.js.bak` is a leftover, superseded backup of `js/seed.js` (confirmed via diff — older/incomplete vocab) sitting in version control with no functional purpose. Remove it. Effort: trivial. Files: `js/seed.js.bak` (delete).

5. **Docs: README's "Integrating with your existing repo" section is stale.** It describes migrating from an original single-file app (legacy `localStorage` keys like `groq_key`/`openai_key`, a "v1.2 folder" not yet promoted to repo root) that no longer exists in this repo — there's no legacy file left to migrate from, and the modular app already *is* the repo root. Either remove this section or mark it as historical/completed so it doesn't send future-you looking for a migration that already happened. Effort: trivial. Files: `README.md`.

6. **Data hygiene: deleting vocab/units leaves orphaned progress rows.** Neither `store.js`'s `deleteVocab`/`deleteUnit` nor the schema clean up matching `progress` rows (progress references `item_id` as free text, not a FK to `vocab_id`, so there's no cascade). Harmless today but accumulates dead rows over time as the curriculum gets edited. Low priority — worth a small cleanup pass either in `deleteVocab`/`deleteUnit` (app-side) or as a periodic Supabase cleanup query. Effort: small. Files: `js/store.js`, `schema.sql`.

## 2026-07-10 (second pass) — implemented (commit e97292d)

Deeper follow-up review at Luke's request, after the first six items above were implemented. Covers ground not fully explored in the first pass: CSS, seed vocab content, and edge cases in the editor/grading flows.

1. **Bug risk: no double-submit guard on quiz/sentence grading.** `gradeAnswer`/`gradeSentence` in `js/pages/runner.js` don't disable the input/Check button while a grading call is in flight (the chat UI already does this via `setBusy()` in `chatui.js` — this doesn't). A double-click, or hitting Enter then clicking Check, can fire two concurrent `quizCall`s for the same question, each calling `recordResult` → `applyAnswer` — inflating `times_seen`/`times_correct` and potentially advancing (or dropping) the SRS ladder twice for one answer. Fix: disable input + button for the duration of the grading call, mirroring `chatui.js`'s pattern. Effort: small. Files: `js/pages/runner.js`.

2. **Bug risk: a unit with zero vocab can permanently softlock all curriculum progress.** `maybePromote()` returns `false` whenever a unit's vocab list is empty, so such a unit can never be marked complete — and `getActiveUnit()` always returns the first non-complete unit in course order, so every unit after it would be permanently unreachable as "up next." This doesn't happen in the current seed data (all 40 units have 14-21 vocab items), but the Curriculum editor's "+ Unit" button happily creates a title-only unit with no vocab yet — a natural "stub now, fill in later" workflow that would trigger exactly this softlock if that stub becomes the active unit before vocab is added. Fix: treat a vocab-less unit as trivially complete (nothing to master) in `maybePromote`/`getActiveUnit`, or warn in the editor. Effort: small. Files: `js/store.js` or `js/pages/runner.js` (`maybePromote`), `js/pages/editor.js`.

3. **Bug risk: unit mastery threshold isn't validated in the Curriculum editor.** The threshold field is `<input type="number" min="0" max="1">`, but `min`/`max` are only soft hints — typing "8" instead of "0.8" (an easy slip) saves verbatim via `parseFloat()` with no clamping. Since `mastery_score` maxes out at 1.0, a threshold above 1 makes that unit's completion mathematically unreachable forever, with no warning telling you why it never clears. Fix: clamp to `[0,1]` on save, or reject/flag out-of-range values inline. Effort: small. Files: `js/pages/editor.js`.

4. **Content quality: ~22 vocab terms duplicated verbatim across different units in the seed curriculum** (44 of 638 rows — e.g. "Ich bin", "die Wohnung", "die Fahrkarte", "Ich möchte", "die Familie" each appear in two separate units, confirmed via a text scan). Each duplicate is a separate, untracked-together vocab row, so mastering a word in an earlier unit doesn't carry over — it gets redrilled from scratch when its duplicate resurfaces later, quietly eating into the ~5-word session slice with something you already know. Also a minor risk for `resolveVocabByLabel`'s substring-matching heuristic when two identical terms exist. Fix: dedupe — keep the earlier (lower-level) occurrence, remove or replace the later one with a genuinely new word. Effort: small. Files: `js/seed.js`.

5. **UX clarity: "review mode" silently runs the full unit vocabulary, not the ~5-word slice a regular lesson uses.** `startSession()` only slices vocab down to `SESSION_SIZE` when `mode === 'curriculum'`; picking a unit in "Review mode" from the Today tab pulls its entire vocab list (14-21 items in the current curriculum) straight into the vocabulary-matching phase — well past the app's own "~5 minute lesson" design target, with no heads-up on the review picker that this is a bigger scope than a normal lesson. Fix: either slice review-mode vocab the same way, or add a short caption near the review picker noting it runs the full unit. Effort: small. Files: `js/pages/runner.js`.

6. **Luke-requested: some vocab words were combined into a single line item (e.g. "eins, zwei, drei", "heute / morgen / gestern") — split each into its own entry, and clean up existing Supabase/localStorage history to match.** Found 24 such combined entries across the seed curriculum (comma- or slash-joined lists of genuinely separate words/forms — distinguished from single phrases that merely contain a comma, like "ich denke, dass" or "Klar, gerne!", which were left alone) and split them into 54 individual rows in `js/seed.js`. Added `splitCombinedVocab()`/`splitCombinedVocabForAllUsers()` to `js/store.js` — a migration (wired into a new Settings admin card) that finds any existing account's vocab rows still holding the old combined text, creates the split rows, evenly divides `times_seen`/`times_correct` across them, copies `srs_level`/`mastery_score`/schedule as-is to each (nothing to subdivide there), and deletes the old row. Also, while auditing for combined items, found and fixed 25 separate exact-duplicate vocab terms scattered across unrelated units (e.g. "die Familie" in two different units as untracked-together rows) — deduped in `seed.js`, keeping the better-fitting occurrence (e.g. "gegessen" now lives only in the irregular-verb unit, correcting a minor mis-categorization). Effort: medium. Files: `js/seed.js`, `js/store.js`, `js/pages/settings.js`.

**Follow-up (same day, commit 23fff84):** Luke asked for the item-4 duplicate fix to also apply to his existing account's live data, not just `seed.js`, and offered Supabase credentials. The sandbox this runs in only has network access to `github.com` (verified: it can't resolve or reach `supabase.co` at all — not a permissions issue, an infrastructure allowlist), so direct database access wasn't possible even with valid credentials. Built the same kind of migration used for item 6 instead: `mergeDuplicateVocab()`/`mergeDuplicateVocabForAllUsers()` in `js/store.js`, wired into a new Settings admin card, for Luke to run himself from his browser (which has normal internet access). Merges each duplicate group into one row — summing review counts, taking the best mastery/schedule state, unioning known errors — using the same keep/remove decisions as the seed.js dedupe, falling back to "keep the oldest row" for any duplicate not in that known list (e.g. one added manually). Safe to re-run.


## 2026-07-11 — implemented (all 4 approved same-day, commit f6958f4)

Fresh review after yesterday's two passes (6 + 6 items, all implemented). Checked each prior fix against current code first — the vocab-softlock fix, threshold clamp, double-submit guards, sentence-phase retry, timeout handling, and the seed dedupe/split/merge migrations are all present and working as described. No re-proposals below; these are new findings from a deeper pass over ai.js, chatui.js, conversation.js, store.js, settings.js, and schema.sql.

1. **Bug: the Conversation tab never logs a session, so free-practice chats don't count toward Stats' streak/session totals.** `js/pages/runner.js` calls `store.createSession`/`updateSession` for curriculum and review-mode lessons, but `js/pages/conversation.js` (the free-practice tab — one of the app's two main daily-practice surfaces) never calls it at all. Stats' "Habit" card (day streak, session count, "Conversations" count) is built entirely from the `sessions` table, so a day spent only in free conversation shows as a broken streak / zero sessions even though real practice happened. Fix: create a session row when a free-conversation chat opens, and close it (mode `'free'`, already a valid value per `schema.sql`) on exit, mirroring the runner's pattern. Effort: small. Files: `js/pages/conversation.js`, `js/store.js`.

2. **UX: Settings has accumulated three permanent "Admin" cards for one-time migrations that have already run.** The sync/split/merge admin cards (curriculum sync, split combined vocab, merge duplicate vocab) were added for specific past cleanups and are now mostly inert — but they're full-height cards sitting between the API keys and the bottom of a page Luke opens for routine handle/key changes. Consider collapsing them under a single collapsed "Maintenance" `<details>` section (they're still safe to keep since they're idempotent) so routine settings edits aren't buried under three paragraphs of migration history. Effort: small. Files: `js/pages/settings.js`.

3. **Cleanup: TTS audio blob URLs are never revoked, leaking memory over a long session.** `speak()` in `js/ai.js` returns a fresh `URL.createObjectURL()` per call, and `chatui.js`'s `say()` (called automatically on every tutor reply, since replies auto-speak) never calls `URL.revokeObjectURL` on the previous one. Harmless in a short lesson, but a long free-conversation session or marathon quiz run accumulates unreleased blob URLs/audio buffers for the rest of the tab's life. Fix: track the last object URL and revoke it before creating the next one (or on `Audio.onended`). Effort: small. Files: `js/chatui.js`, `js/ai.js`.

4. **Data: `sessions.items_reviewed` / `items_introduced` / `errors_observed` are defined in `schema.sql` but never written.** `store.createSession` only ever sets `mode`/`unit_id`/`outcome`/timestamps — these three JSONB columns stay `[]` forever. Not causing any bug today (Stats recomputes everything from `progress` instead), but it's dead schema surface that implies richer per-session history exists when it doesn't. Either wire them up (record which items were graded/introduced each session, enabling a future "session history" view) or drop the columns to cut confusion. Lower priority than 1–3. Effort: medium if wiring up, trivial if dropping. Files: `js/store.js`, `js/pages/runner.js`, `schema.sql`.

## 2026-07-11 (second pass) — implemented (all 4 approved same-day, commit 8d90b04)

Deeper follow-up at Luke's request: (1) review what informs the quality of generated sentences/quiz items, (2) identify practice methods the app doesn't currently use. Checked today's first-pass items against current code first — all four are in place and working.

1. **Quality: sentence-drill and quiz generation never see each unit's `grammar_focus`, and the quiz generator additionally drops vocab `notes`.** `seed.js` authors a specific, well-targeted grammar point for all 39 units (e.g. "position of the past participle," "word order with wenn," "Konjunktiv II for polite requests") — but `buildSentencePrompt`/`buildQuizPrompt` in `js/prompts.js` only ever build their item pool from bare `german — english` pairs. `grammar_focus` is passed to `buildUnitConvoPrompt` (phase 3) only, so the two actually-graded phases (phase 2 sentences, phase 4 quiz — the ones testing recall, not just conversation) have no idea what grammar point the unit is teaching and default to simple, ungrounded sentences built only from the vocab list. Separately, `buildQuizPrompt`'s item-pool lines strip each item's `.notes` field entirely (`buildSentencePrompt`'s pool keeps it) — losing hints like "Triggers normal verb-second order, unlike weil" that the model needs to write a correct fill-in-the-blank around that exact point. `weakPoints`' `known_errors` (why a word is flagged weak) is similarly available but never passed into quiz generation. Fix: thread `grammar_focus` into both prompts and instruct the generator to build at least 1-2 items that specifically exercise it; restore `.notes` (and `known_errors`) into the quiz pool lines to match the sentence prompt. Effort: small-medium. Files: `js/prompts.js`.

2. **New practice method — listening dictation.** `speak()` (OpenAI TTS) and `transcribe()` (Whisper) are already fully wired for the Conversation tab's mic + auto-speak, but nothing in the app tests listening comprehension in isolation — tutor replies just auto-play passively. Meanwhile `cefr.js` reports a distinct "Listening" skill score that's actually derived from the same activity signal as reading (no listening-specific data feeds it — by the code's own honest framing, "a motivating gauge, not a placement test," but it'd be a truer gauge with real input). Add a dictation item type: play a German sentence via TTS, learner types (or speaks, via the existing mic) what they heard, grade against the source text using the same lenient grader. Would also make the CEFR listening component mean something. Effort: medium. Files: `js/prompts.js` (new item type), `js/pages/runner.js` (wire into a phase), `js/ai.js` (already has what's needed).

3. **New practice method — word-order scramble drill.** At least 5 units' `grammar_focus` is specifically about word order ("position of the past participle," "question word order," "verb-second vs. verb-final," "word order with wenn," "indirect speech word order"), but no current exercise isolates word order as its own skill: vocab matching tests recognition, sentence drills test free production (where a lenient grader may let word-order slips through if the meaning still lands), and `fill_blank` only ever blanks one word. A scrambled-sentence-reorder exercise (tap German words/chunks into the correct sequence) would directly and unambiguously test exactly what those units are trying to teach, generated from the same LLM sentence pool already in use. Effort: medium. Files: `js/prompts.js`, `js/pages/runner.js`, `css/styles.css`.

4. **New practice method — client-side multiple-choice recognition round for brand-new words.** Every current rep (sentences, quiz) is a full LLM round-trip requiring free recall/typing — appropriately hard for consolidation, but slow and API-cost-heavy for a word's very first exposure. A quick, no-API multiple-choice round (3-4 distractors pulled from other vocab already loaded in the curriculum, all client-side) between vocab-matching and sentence drills would give brand-new words a cheap, fast recognition rep before the harder production reps, and reduce LLM calls per lesson. Effort: small-medium. Files: `js/pages/runner.js`.

## 2026-07-12 — implemented (all 3 approved same-day, commit c996377)

Review of everything added since the last logged pass: the Exercises tab, Verb Conjugation Match, the Free Conversation move, the content-feedback flagging system, and version tracking. Re-checked the matching-logic fix (text-based, not id-based — confirmed intact) and the feedback plumbing (defensive `.catch(() => [])` on every fetch — confirmed intact). No new bugs found in that new code itself; today's findings are one gap in it, one loose end from a previous fix, and one small UX consistency item.

1. **Bug risk: Verb Conjugation Match doesn't dedupe rounds by verb.** `mount()` in `js/exercises/conjugationMatch.js` slices the LLM's returned `table.verbs` straight to `ROUND_SIZE` with no check for the same underlying verb appearing twice. Today's seed data happens not to trigger it (checked all 10 bare-infinitive entries against the ~20 "Ich [verb]" phrase entries — no overlaps), but nothing stops it if seed data changes, or if the LLM itself ever returns the same verb from two different candidate lines. Fix: dedupe by normalized `infinitive` before slicing. Effort: small. Files: `js/exercises/conjugationMatch.js`.

2. **Loose end: the listening-dictation item type never actually fed the CEFR "Listening" score.** The 2026-07-11 (second pass) item that added `listen_type` items stated its goal as "Would also make the CEFR listening component mean something" — but `cefr.js` was never touched. `estimateLevels()` still derives `listening` from the same blended overall `accuracy` figure as reading and speaking (`base * 0.95`), with no signal specific to dictation performance. The exercise itself works and is a genuine addition, but the stated CEFR benefit was never wired up. Fix: track accuracy on `listen_type` items separately (e.g. via `item_type` on the progress row) and feed that into the listening component instead of blended overall accuracy. Effort: small-medium. Files: `js/cefr.js`, `js/store.js`, `js/pages/runner.js`.

3. **UX: API-key errors don't point at the fix.** `ai.js` throws `"Missing API key. Add it in Settings."` / `"Missing OpenAI key. Add it in Settings."`, but the error screens that surface these (sentence/quiz phases in `runner.js`, Conjugation Match in `conjugationMatch.js`) only ever offer "Try again," which just fails the same way again. Detect the "Add it in Settings" text and show a "Go to Settings" button in that case instead. Effort: small. Files: `js/pages/runner.js`, `js/exercises/conjugationMatch.js`.

**Implemented as:** item 1 — dedupe by normalized `infinitive` (falling back to the source vocab's German text) before the `ROUND_SIZE` slice. Item 2 — added a dedicated aggregate progress row (`item_type: 'listening'`, `item_id: 'listening:aggregate'`) written independently of the per-vocab `item_label` mapping (dictation items test a whole heard sentence, not one word, so relying on label-resolution alone would undercount); `cefr.js` now swaps its blended-accuracy term for this listening-specific accuracy once there are ≥3 reps, falling back to blended accuracy before then to avoid a 1-2-item sample swinging the score. Item 3 — both `runner.js` error screens (sentence phase, quiz phase) and `conjugationMatch.js`'s now detect "Add it in Settings" and swap the dead-end "Try again" for a "Go to Settings" button. v1.0.0 → v1.0.1.

## 2026-07-13 — implemented (all 4 approved same-day, commit 0c1ffbb)

Fresh pass focused on technical/pedagogical depth per Luke's standing preference. Checked every prior fix against current code first — the level-A1 tutor prompt hardcode below is new; everything else logged through 2026-07-12 (dedupe, listening→CEFR, key-error UX, softlock/threshold/orphan fixes, timeouts, double-submit guards, maintenance collapsing, blob URL revocation, free-conversation session logging) is confirmed present and working. No re-proposals.

1. **Bug: the curriculum conversation phase always tells the tutor model the learner is "roughly A1–A2," even in units whose `grammar_focus` is clearly B2–C1** (e.g. unit-level topics in `seed.js` include passive voice, genitive case, nominalization patterns, Konjunktiv I/II, indirect speech, concessive connectors, "extended attributes," advanced connectors like *zumal/indes/mithin*). `buildUnitConvoPrompt` in `js/prompts.js` hardcodes "You are a warm, encouraging German conversation tutor for an adult learner (Luke) at roughly A1–A2 level... Keep your German at or just slightly above the learner's level" regardless of which unit is active — so by the back half of the curriculum the tutor is instructed to stay simple in the exact phase meant to weave in that unit's (advanced) vocabulary and grammar into real conversation. `cefr.js` already computes a live level estimate (`estimateLevels`) that could supply a real per-session level instead of a fixed string. Fix: thread a computed level (from CEFR estimate, or more simply from the unit/section's position in the curriculum) into `buildUnitConvoPrompt`, replacing the fixed "A1–A2" text. Effort: small-medium. Files: `js/prompts.js` (`buildUnitConvoPrompt`), `js/pages/runner.js` (`buildContext`, to pass the level through).

2. **Content quality: the quick recognition round's multiple-choice distractors are drawn from the entire curriculum's vocab, not vocab near the current unit's level.** `buildRecognitionOptions` in `js/pages/runner.js` pools every vocab item across all 40 units as candidate distractors for a brand-new word's first-exposure check. Early lessons can get distractors lifted from unit 35+ ("nominalized verbs," idiomatic particles) that are so obviously mismatched in register they're never seriously considered — and the reverse for late units, where basic A1 distractors don't test whether the learner actually grasped a new advanced term. This blunts the one modality (per the 2026-07-11 second-pass item that added this round) meant to give brand-new words a cheap, real recognition rep. Fix: constrain the distractor pool to vocab from nearby units (e.g. same section, or +/- a few units) instead of the full curriculum. Effort: small. Files: `js/pages/runner.js` (`buildRecognitionOptions`).

3. **Cleanup: dead code — a plain-text tutor prompt/call path that nothing uses anymore.** `buildTutorPrompt` (`js/prompts.js`) and `tutorReply` (`js/ai.js`) are both exported and documented as "kept for any non-structured caller," but no such caller exists — every conversation surface (Free Conversation, the curriculum conversation phase) uses the structured JSON path (`buildUnitConvoPrompt`/`buildFreeConvoPrompt` + `tutorStructured`) exclusively. Confirmed via a full-repo search: neither function is imported anywhere. Remove both, or fold a note into the comment if there's a reason to keep them staged for a future non-JSON caller. Effort: trivial. Files: `js/prompts.js`, `js/ai.js`.

4. **Cleanup: `content_feedback.status` is written but never read.** `createContentFeedback` (`js/store.js`) always writes `status: 'open'`, and the schema/comments imply an open/resolved workflow, but nothing anywhere ever transitions a row out of `'open'` or filters on status — `recentFeedbackNotes` ranks purely by recency. Harmless today since the recency cap (6 most-recent notes) already bounds prompt bloat, but the field currently implies a triage workflow that doesn't exist. Either wire up marking old notes resolved once the underlying content issue is fixed, or drop the field. Effort: trivial. Files: `js/store.js`, `schema.sql`.

## 2026-07-14 — implemented (all 4 approved same-day, commit bc2a6ba)

Fresh pass, technical/pedagogical focus per Luke's standing preference. Checked prior fixes against current code first: level-aware tutor prompt, section-scoped recognition distractors, dead tutorReply/buildTutorPrompt removal, and the content_feedback status filter are all present and working. Verified via a scripted scan of `seed.js` that the vocab-dedup/combined-vocab work has held — 0 duplicate German terms across all 40 units, every unit has non-empty `grammar_focus`/`objectives`, all ≥8 vocab items, and every section's `notes` still carries a parseable CEFR band. No re-proposals; today's items are new gaps found in the feedback/telemetry plumbing and one stats mislabel.

1. **Data quality: flagged content-feedback notes never expire, so a long-fixed issue keeps steering generation forever.** The 2026-07-13 fix added `resolveContentFeedback(id, status)` to `store.js` and made `recentFeedbackNotes` skip anything not `status: 'open'` — but nothing anywhere ever calls `resolveContentFeedback`. Every note Luke flags via "⚑ Something wrong with this?" stays `'open'` permanently, so `pastIssuesBlock` in `prompts.js` keeps feeding it back as a "known issue to avoid" indefinitely, even for a problem fixed months ago in `seed.js`/`prompts.js` — the recency cap (6 most-recent) bounds the damage but doesn't fix it, since an old open note only falls off once 6 *newer* opens accumulate, not because it was addressed. Fix: a small admin affordance in Settings' Maintenance section listing open feedback (prompt/note/date) with a "mark resolved" button per row, or per-context "resolve all older than today" bulk action. Effort: small-medium. Files: `js/pages/settings.js` (new UI), `js/store.js` (already has the update fn).

2. **Data quality: session telemetry (`items_introduced`/`items_reviewed`/`errors_observed`) has been written every session since 2026-07-11 but nothing ever reads it.** `endSession()` in `runner.js` faithfully populates all three JSONB columns per the 2026-07-11 fix, but a repo-wide search turns up zero readers — `stats.js` still derives everything from `progress`/`sessions.started_at` alone. The data's accumulating in Supabase/localStorage with no consumer, which was explicitly flagged as a risk in that same log entry ("enabling a future 'session history' view") that never got built. Either surface it — a "recent sessions" list in Stats showing what was introduced/reviewed and which error types came up each session, which would also make the app's own error-type tagging (`spelling`/`umlaut`/`vocab`/`grammar`) visible to Luke for the first time instead of only feeding `known_errors` silently — or stop writing it if it's not worth surfacing. Effort: small (drop) to medium (build the view). Files: `js/pages/stats.js` (new section), or `js/store.js`/`js/pages/runner.js`/`schema.sql` (to drop).

3. **Stats mislabel: the "Conversations" count (and the CEFR speaking boost it feeds) actually counts curriculum lessons too, not just free-conversation practice.** `stats.js` computes `conversationSessions = sessions.filter(s => s.mode !== 'review').length` — since `sessions.mode` is `curriculum | review | free`, this lumps every curriculum lesson session in with actual Free Conversation sessions under the "Conversations" stat, and the same blended number feeds `cefr.js`'s `speaking = base * 0.88 + min(8, conversationSessions * 0.5)` bonus. A learner who never opens Free Conversation but runs daily curriculum lessons sees a "Conversations" count and a speaking-skill bonus that implies free-form speaking practice that never happened (curriculum lessons do include a conversation phase, so it's not baseless — but the label and the CEFR framing both read as free-practice-specific). Fix: split into three real counts (curriculum / free / review) or rename the stat/comment to reflect what it actually measures. Effort: small. Files: `js/pages/stats.js`, `js/cefr.js`.

4. **Cleanup: `vocab.tags` is defined in `schema.sql`, accepted by `store.createVocab`, and always initialized to `[]` — but nothing anywhere sets it to anything, reads it, or exposes it in the Curriculum editor.** Same "implied workflow that doesn't exist" pattern as the `content_feedback.status` field flagged on 2026-07-13: a real jsonb column with no writer beyond the empty default and no reader. Either wire up a real use (e.g. tag vocab by grammar concept — genders, separable verbs, cases — for filtering/distractor-pool scoping) or drop the column to stop implying a feature that isn't there. Effort: trivial (drop) to medium (build tagging UI + a real use). Files: `schema.sql`, `js/store.js`, `js/pages/editor.js`.

**Implemented as:** item 1 — added a "Flagged content" admin card in Settings' Maintenance section listing every open `content_feedback` note with a "Mark resolved" button per row (calls the existing `resolveContentFeedback`). Item 2 — kept writing session telemetry as-is; added `store.recentErrorTrend()` (aggregates `errors_observed` error-type counts across the last 10 sessions) and threaded it into `buildSentencePrompt`/`buildQuizPrompt`/`buildUnitConvoPrompt` as a new "RECENT ERROR TREND" instruction block so generation now actually leans toward a learner's recurring weak spots, not just per-word `known_errors`; also added a "Recent sessions" card to Stats showing each of the last 8 sessions' mode/unit, items introduced/reviewed, and error-type tally — the first UI reader of that data. Item 3 — split the blended "Conversations" session count into real `lessonSessions`/`freeSessions`/`reviewSessions` (Stats' Habit card now shows all three, honestly labeled); `cefr.js`'s speaking bonus keeps combining lesson+free sessions (both are real spoken production) under a clearly-named variable instead of an overloaded "conversations" one. Item 4 — dropped `vocab.tags` (schema.sql column + the `alter table drop column if exists` for already-provisioned databases, and the dead field in `store.createVocab`) rather than building a tagging UI, since no concrete use was identified beyond the original speculative default — a real tagging feature is a bigger, separate proposal if wanted later. v1.0.2 → v1.0.3.
